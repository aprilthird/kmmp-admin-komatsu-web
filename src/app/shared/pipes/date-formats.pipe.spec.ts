import { DateFormatsPipe } from './date-formats.pipe';

describe('DateFormatsPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatsPipe();
    expect(pipe).toBeTruthy();
  });
});
