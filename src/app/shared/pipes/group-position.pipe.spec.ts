import { GroupPositionPipe } from './group-position.pipe';

describe('GroupPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
